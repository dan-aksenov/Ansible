
- hosts: all
  user: dbax
  become: yes

  roles:
      - pgdg
      - postgres_install 
      - postgres_config
      - mamonsu
      - pg_agent
      - pg_bouncer
      - oraclefdw
      - plpgsql_check
