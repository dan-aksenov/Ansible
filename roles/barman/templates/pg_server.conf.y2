["{{ pg_server  }}"]
description =  "{{ pg_server  }}" database server
conninfo = host="{{ pg_server  }}"oe-lkk-db user=barman dbname=postgres
backup_method = postgres
streaming_conninfo = host="{{ pg_server  }}" user=streaming_barman dbname=postgres
streaming_archiver = on
slot_name = barman

