# configured via ansible
BOOTPROTO="none"
DEVICE="{{ if_name.stdout }}"
ONBOOT=yes
TYPE=Ethernet
IPADDR="{{ host_ip.stdout }}"
NETMASK="{{ netmask }}" 
GATEWAY="{{ gateway }}" 
DNS1="{{ dns }}"
DOMAIN="{{ domain }}"
NM_CONTROLLED=no
NAME="{{ if_name.stdout }}"
"{{ hardware_address }}"
