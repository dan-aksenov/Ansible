---

- name: Installing packages
  yum: name={{ item }}
  with_items:
      # DEV tools
      - git
      - gcc
      - make
      - rpm-build
      - gdb 
      
      # PY tools
      - python-pip 
      - python-devel
      - python-setuptools
      - libselinux-python
      
      # MON tools
      - hdparm
      - iotop
      - sysstat
      # I have special role for atop, but let be here for list completeness.
      - atop
      - htop
      # Logfile browser
      - lnav
      - lshw
      - strace
      - glances
      - lsof
      # A set of utilities that use the proc filesystem (fuser, killall, pidof, pstree)
      - psmisc
      # tools and drivers for monitoring temperatures, voltage, and fans.
      - lm_sensors
      
      # MISC tools
      - vim
      - open-vm-tools
      # Database for local files
      - mlocate
      - rsync
      - zip
      - unzip
   
      # Network tools
      - net-tools
      - iptraf    
      - nethogs
      - tcpdump
      - iperf
      - mtr
      - iftop
      - wget
      - ntpdate      
      - ngrep
      - bind-utils
        
...
